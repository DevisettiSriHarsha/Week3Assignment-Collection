package org.cg.employeemanagement.service;

import java.util.Comparator;

import org.cg.employeemanagement.model.EmpAddress;
import org.cg.employeemanagement.model.EmployeeDetails;

public class EmployeeDetailsSorting {
	
	/*
	 * public static final Comparator<EmployeeDetails> COMPARE_BY_EMP_ID =
	 * Comparator.comparing(EmployeeDetails::getEmployeeId); public static final
	 * Comparator<EmployeeDetails> COMPARE_BY_EMP_FIRST_NAME =
	 * Comparator.comparing(EmployeeDetails::getFirstName); public static final
	 * Comparator<EmployeeDetails> COMPARE_BY_EMP_LAST_NAME =
	 * Comparator.comparing(EmployeeDetails::getLastName); public static final
	 * Comparator<EmployeeDetails> COMPARE_BY_EMP_SALARY =
	 * Comparator.comparing(EmployeeDetails::getSalary); public static final
	 * Comparator<EmployeeDetails> COMPARE_BY_EMP_DEPARTMENT = new
	 * Comparator<EmployeeDetails>() { public int compare(EmployeeDetails emp1,
	 * EmployeeDetails emp2) { return
	 * emp1.getEmpDepartment().getDepartmentId().compareTo(emp2.getEmpDepartment().
	 * getDepartmentId()); } }; public static final Comparator<EmpAddress>
	 * COMPARE_BY_EMP_ADDRESS = Comparator.comparing(EmpAddress::getAddressId);
	 */
}
